# Enable after config
ssl = no
#ssl_protocols = !SSLv2:!SSLv3
#ssl_dh_parameters_length = 2048

# Maybe add more to this later.
#ssl_cipher_list = TLSv1.2:TLSv1:AES256:SHA256:SHA384:HIGH
#ssl_prefer_server_ciphers = yes
#ssl_options = no_compression

# Uncomment after config.
#ssl_cert = </etc/pki/certs/cert.pem
#ssl_key = </etc/pki/private/cert.pem
#ssl_key_password = </etc/pki/private/cert.key

# PEM encoded trusted certificate authority. Set this only if you intend to use
# ssl_verify_client_cert=yes. The file should contain the CA certificate(s)
# followed by the matching CRL(s). (e.g. ssl_ca = </etc/pki/dovecot/certs/ca.pem)
#ssl_ca = 

# Require that CRL check succeeds for client certificates.
#ssl_require_crl = yes

# Directory and/or file for trusted SSL CA certificates. These are used only
# when Dovecot needs to act as an SSL client (e.g. imapc backend). The
# directory is usually /etc/pki/dovecot/certs in Debian-based systems and the file is
# /etc/pki/tls/cert.pem in RedHat-based systems.
#ssl_client_ca_dir =
#ssl_client_ca_file =

# Request client to send a certificate. If you also want to require it, set
# auth_ssl_require_client_cert=yes in auth section.
#ssl_verify_client_cert = no

# Which field from certificate to use for username. commonName and
# x500UniqueIdentifier are the usual choices. You'll also need to set
# auth_ssl_username_from_cert=yes.
#ssl_cert_username_field = commonName
